{
	"name": "tilemapper",
	"version": "1.0.0",
	"description": "Generate a tilemap from multiple images",
	"scripts": {
		"test": "tsc && node --enable-source-maps dist"
	},
	"devDependencies": {
		"@types/node": "^14.0.1",
		"@types/sharp": "^0.25.0",
		"typescript": "^3.9.2"
	},
	"dependencies": {
		"sharp": "^0.25.2",
		"tslib": "^2.0.0"
	}
}
